{
  "name": "csspad",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "clean": "rm -rf ./dist",
    "format": "biome format --write ./src"
  },
  "devDependencies": {
    "sass": "^1.99.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "vite-plugin-biome": "^1.2.0"
  }
}
